// written by fabiantheblind // http://www.the-moron.net // see circle.jsx for detailed commentsvar doc  = app.documents.add();doc.documentPreferences.properties = {     pageWidth  : 160,     pageHeight : 160     }; var bounds = 10;var distFactor = 3;var ph  = doc.documentPreferences.pageWidth;var pw = doc.documentPreferences.pageHeight;var p = doc.pages[0]; var ovs = new Array(); for(var i = 0; i < 20 ; i++){ 	var y1 = bounds + (i * distFactor);	var x1 = bounds + (i * distFactor);	var y2 = ph - bounds - (i * distFactor);	var x2 = pw - bounds - (i * distFactor);	    ovs[i] = p.ovals.add({						geometricBounds:[y1,x1,y2,x2]						});     ovs[i].strokeWeight = 0;     ovs[i].textPaths.add();     if(i > 0){         ovs[i].textPaths[0].previousTextFrame = ovs[i-1].textPaths[0];         }     } ovs[0].textPaths[0].contents = TextFrameContents.placeholderText;